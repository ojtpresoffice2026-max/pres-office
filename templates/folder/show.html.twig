{% extends 'base.html.twig' %}

{% block title %}Folder{% endblock %}

{% block body %}
    <h1>Folder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ folder.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ folder.name }}</td>
            </tr>
            <tr>
                <th>Scanned_folder_no</th>
                <td>{{ folder.scannedFolderNo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_folder_index') }}">back to list</a>

    <a href="{{ path('app_folder_edit', {'id': folder.id}) }}">edit</a>

    {{ include('folder/_delete_form.html.twig') }}
{% endblock %}
