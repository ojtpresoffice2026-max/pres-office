{% extends 'base.html.twig' %}

{% block title %}Shelf{% endblock %}

{% block body %}
    <h1>Shelf</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shelf.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shelf.name }}</td>
            </tr>
            <tr>
                <th>Transfered_date</th>
                <td>{{ shelf.transferedDate ? shelf.transferedDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_shelf_index') }}">back to list</a>

    <a href="{{ path('app_shelf_edit', {'id': shelf.id}) }}">edit</a>

    {{ include('shelf/_delete_form.html.twig') }}
{% endblock %}
