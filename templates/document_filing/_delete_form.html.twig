<form method="post" action="{{ path('app_document_filing_delete', {'id': document_filing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document_filing.id) }}">
    <button class="btn">Delete</button>
</form>
