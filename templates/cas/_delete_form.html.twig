<form method="post" action="{{ path('app_cas_delete', {'id': ca.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ca.id) }}">
    <button class="btn">Delete</button>
</form>
