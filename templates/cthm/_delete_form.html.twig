<form method="post" action="{{ path('app_cthm_delete', {'id': cthm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cthm.id) }}">
    <button class="btn">Delete</button>
</form>
