<form method="post" action="{{ path('app_cba_delete', {'id': cba.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cba.id) }}">
    <button class="btn">Delete</button>
</form>
