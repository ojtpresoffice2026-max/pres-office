<form method="post" action="{{ path('app_cit_delete', {'id': cit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cit.id) }}">
    <button class="btn">Delete</button>
</form>
