<form method="post" action="{{ path('app_caff_delete', {'id': caff.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caff.id) }}">
    <button class="btn">Delete</button>
</form>
